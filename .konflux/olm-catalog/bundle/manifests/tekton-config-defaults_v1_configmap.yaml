apiVersion: v1
data:
  AUTOINSTALL_COMPONENTS: "true"
  DEFAULT_TARGET_NAMESPACE: openshift-pipelines
  version: null
kind: ConfigMap
metadata:
  labels:
    operator.tekton.dev/release: "0.79.1"
  name: tekton-config-defaults
spec:
  install:
    spec:
      deployments: []
