name: operator-update-images

on:
  pull_request:
  push:
    branches:
      - next
      - main
      - release-v*
    paths:
      - project.yaml
  workflow_dispatch:
    inputs:
      release_stage:
        description: 'Select Environment'
        required: false
        type: choice
        options:
          - devel
          - staging
          - production

jobs:
  update-csv:
    runs-on: ubuntu-latest
    if: github.repository_owner == 'openshift-pipelines' # do not run this elsewhere
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout the current repo
        uses: actions/checkout@v5

      - name: Setup Release Stage
        uses: ./.github/actions/set-release-stage
        with:
          # Pass the workflow dispatch input to the action
          manual_stage: ${{ github.event.inputs.release_stage }}

      - name: update-image-references
        run: |
          set -ex  
          # Update operator's payload and stuff
          make ENVIRONMENT=$STAGE update-reference  

      - name: Commit new changes
        run: |
          BASE_BRANCH=${GITHUB_REF#refs/heads/}
          SOURCE_BRANCH=actions/update/operator-update-images-${BASE_BRANCH}
          git config user.name openshift-pipelines-bot
          git config user.email pipelines-extcomm@redhat.com
          git checkout -b ${SOURCE_BRANCH}
          
          git add -f  head .konflux olm .github
          
          if [[ -z $(git status --porcelain --untracked-files=no) ]]; then
            echo "No change, exiting"
            exit 0
          fi
          
          git commit -F- <<EOF
            [bot:${BASE_BRANCH}] Update  generate Cluster Service Version (CSV)
          EOF
          
          git push -f origin ${SOURCE_BRANCH}
          pr_list=$(gh pr list --base ${BASE_BRANCH} --head ${SOURCE_BRANCH} --json number)
          echo "Waiting for 10s before updating PRs"
          sleep 10
          if [ "$(echo $pr_list | jq length)" = "0" ]; then
            echo "creating PR..."
            gh pr create -B ${BASE_BRANCH} -H ${SOURCE_BRANCH} --label=automated --label=lgtm --label=approved  --fill
          else
            echo "a PR already exists, editing..."
            gh pr edit $(echo $pr_list | jq .[0].number) --add-label=lgtm  --add-label=approved 
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
